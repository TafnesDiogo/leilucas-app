import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "br.com.unisagrado.soslucas",
  appName: "SOS Lucas",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
